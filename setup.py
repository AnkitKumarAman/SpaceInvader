from setuptools import setup, find_packages

setup(
    name="spaceinvader",
    version="1.0",
    packages=find_packages(),
    install_requires=["pygame"],  # Add other dependencies if needed
)
